import React from 'react';
import PropTypes from 'prop-types';

const Information = ({ dispatch }) =>
  <div>政策信息，暂未开放，敬请期待！</div>;

Information.propTypes = {
  dispatch: PropTypes.func,
};

export default Information;
